# HostelCare Backend

Simple Express server that exposes a health endpoint and is ready for additional API routes.

## Getting started

```bash
cd backend
npm install
npm run dev
```

Copy `.env.example` to `.env` and adjust settings as needed.
